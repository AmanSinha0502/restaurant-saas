export type Owner = {
  _id: string;
  businessName: string;
  email: string;
  phone?: string;
  country?: string;
  active?: boolean;
  createdAt?: string;
  address?: string;
  // add more fields as needed
};
